#File
File One:
  Empty.                            [single][property emptyFile]
  Not empty.
  File not found.                   [error]

File Two:
  Empty.                            [single][property emptyFile]
  Not empty.
  File not found.                   [error]

#Check match
Number of matches of the pattern in second file:
  None.                               [if !emptyFile][property noMatch]
  One.                                [if !emptyFile]
  Many.                               [if !emptyFile]

Number of matches of the pattern in one line:
  One.                                [if !noMatch && !emptyFile]
  Many.                               [if !noMatch && !emptyFile]

#Replace
Replace Value:
  Replace a char.                     [if !emptyFile][property replaceChar]
  Replace a string.                   [if !emptyFile][property replaceString]
