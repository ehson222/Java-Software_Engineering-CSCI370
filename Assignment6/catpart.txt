# File
File One:
  Empty.                            [single][property emptyFile]
  Not empty.
  File not found.                   [error]

File Two:
  Empty.                            [single][property emptyFile]
  Not empty.
  File not found.                   [error]

#Parameter String 1:
  length0.                          [single][property stringEmpty]
  length1.                          [property isChar]
  lengthX.                          [property isString]
  lengthNeg.

# Parameter String 2:
  length0.
  length1.
  lengthX.
  lengthNeg.

# Check match
Number of matches of the pattern in second file:
  None.                               [if !emptyFile][property noMatch]
  One.                                [if !emptyFile && isChar]
  Many.                               [if !emptyFile && isString]

Number of matches of the pattern in one line:
  One.                                [if !noMatch && !emptyFile]
  Many.                               [if !noMatch && !emptyFile]

# Replace
Replace Value:
  Replace a char.                     [if !emptyFile && isChar || isString][property replaceChar]
  Replace a string.                   [if !emptyFile && isChar || isString][property replaceString]
